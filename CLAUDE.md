# easyblog v1-rebuild - 开发规范

**版本**: v1.0  
**创建时间**: 2026-04-01  
**分支**: feature/v1-rebuild

---

## 项目概述

Easyblog 是一个全栈博客系统，包含后端 API 服务和前端管理界面。

**技术栈**:
- 后端：Go 1.23.5 + Gin + GORM + MySQL + Redis
- 前端：Vue 3.4 + Vite + TypeScript + Element Plus
- 部署：Docker + Kubernetes

---

## 任务依赖关系

```
┌─────────────────────────────────────────────────────────────┐
│                    任务依赖关系图                            │
└─────────────────────────────────────────────────────────────┘

v0.1 项目骨架（第 1 周）
    │
    ▼
v0.1.5 基础设施 ⭐ 关键依赖
├── RBAC 权限中间件 ──→ 所有 API 接口依赖
├── JWT 认证中间件 ──→ 用户登录状态依赖
├── Redis 缓存 ──────→ Token 存储依赖
└── 图片上传基础 ────→ 头像/封面图依赖
    │
    ▼
v0.2 用户系统（第 2 周）
├── 依赖：v0.1.5 中间件、Redis
├── 联调：登录/注册流程前后端联调
    │
    ▼
v0.3 文章系统（第 3 周）
├── 依赖：用户系统（作者信息）、图片上传（封面）
├── 联调：文章 CRUD 前后端联调
    │
    ▼
v0.4 互动系统（第 4 周）
├── 依赖：用户系统（评论者）、文章系统（被评论对象）
├── 联调：评论/点赞前后端联调
    │
    ▼
v0.5 管理系统（第 5 周）
├── 依赖：用户/文章/评论数据
├── 联调：管理后台全流程联调
    │
    ▼
v0.6 增强功能（第 6 周）
├── GitHub OAuth（可选功能）
├── OSS 对象存储（图片备份）
└── 缓存优化（性能提升）
    │
    ▼
v1.0 上线准备（第 7 周）
├── API 文档（Swagger）
├── 全量测试（集成/E2E/性能）
├── Bug 修复
└── 运维部署（Docker/K8s/监控）
```

---

## 关键依赖说明

### v0.1.5 基础设施（必须优先完成）

这部分是整个系统的基石，**必须在 v0.2 之前完成**：

| 组件 | 用途 | 影响范围 |
|------|------|---------|
| RBAC 权限中间件 | 接口权限校验 | 所有 API 接口 |
| JWT 认证中间件 | 用户身份认证 | 所有登录态接口 |
| Redis 连接 | Token 存储、缓存 | 用户会话、性能优化 |
| 图片上传基础 | 头像、封面图上传 | 用户模块、文章模块 |

**如果 v0.1.5 未完成，v0.2 的以下功能无法实现**：
- 用户登录（需要 JWT 中间件）
- 用户信息更新（需要权限校验）
- 用户头像上传（需要图片上传功能）
- 用户状态缓存（需要 Redis）

---

## 开发流程

### 1. 按阶段顺序开发

```
每个阶段遵循：后端 API → 前端页面 → 单元测试 → 前后端联调
```

### 2. 联调时机

**不要等到最后才联调**！每个阶段完成后立即联调：

| 阶段 | 联调内容 |
|------|---------|
| v0.2 | 登录、注册、个人中心 |
| v0.3 | 文章列表、详情、编辑器 |
| v0.4 | 评论组件、点赞交互 |
| v0.5 | 管理后台全流程 |

### 3. 测试要求

| 类型 | 工具 | 时机 | 要求 |
|------|------|------|------|
| 后端单元测试 | Go test | 每个函数完成后 | 覆盖率≥80% |
| 前端组件测试 | Vitest | 每个组件完成后 | 核心组件≥80% |
| API 集成测试 | Go test + HTTP | 每个 API 完成后 | 全部通过 |
| E2E 测试 | Playwright | 阶段联调时 | 核心流程 100% |

---

## 核心组件架构

### 后端分层

```
┌─────────────────────────────────────────┐
│             Handler 层                   │  ← HTTP 入口，参数校验
├─────────────────────────────────────────┤
│              Biz 层                      │  ← 业务逻辑，事务处理
├─────────────────────────────────────────┤
│             Store 层                     │  ← 数据访问，CRUD 封装
├─────────────────────────────────────────┤
│             Model 层                     │  ← 数据模型，表结构
└─────────────────────────────────────────┘
```

### 前端分层

```
┌─────────────────────────────────────────┐
│              Views 层                    │  ← 页面组件
├─────────────────────────────────────────┤
│           Components 层                  │  ← 通用组件
├─────────────────────────────────────────┤
│              API 层                      │  ← 接口封装
├─────────────────────────────────────────┤
│             Stores 层                    │  ← 状态管理 (Pinia)
└─────────────────────────────────────────┘
```

---

## 单元测试要求

### 后端测试覆盖率

| 模块 | 覆盖率要求 |
|------|----------|
| Handler 层 | ≥ 80% |
| Biz 层 | ≥ 80% |
| Store 层 | ≥ 80% |
| Model 层 | ≥ 70% |
| Middleware | ≥ 80% |
| 工具函数 | ≥ 90% |

### 测试命名规范

```go
// 格式：Test[功能]_[场景]_[预期结果]
func TestUser_Register_Success(t *testing.T) { ... }
func TestUser_Register_DuplicateUsername(t *testing.T) { ... }
func TestUser_Login_InvalidPassword(t *testing.T) { ... }
```

### 表驱动测试

```go
func TestUser_Register(t *testing.T) {
    tests := []struct {
        name    string
        input   UserInput
        wantErr bool
    }{
        {"成功", validInput, false},
        {"用户名重复", duplicateInput, true},
        {"密码太短", shortPasswordInput, true},
    }
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) { ... })
    }
}
```

---

## 提交前检查清单

### 后端
- [ ] 代码已 `gofmt` 格式化
- [ ] 通过 `go vet` 检查
- [ ] 通过 `golangci-lint` 检查
- [ ] 所有测试通过
- [ ] 覆盖率达标（≥80%）
- [ ] 无敏感信息（密码/密钥）

### 前端
- [ ] 代码已 `prettier` 格式化
- [ ] 通过 `eslint` 检查
- [ ] 组件测试通过
- [ ] 无 TypeScript 类型错误
- [ ] 无控制台警告

### 文档
- [ ] API 文档已更新（Swagger 注释）
- [ ] 变更记录已更新
- [ ] 复杂逻辑有注释说明

---

## 任务追踪

任务列表：`openspec/changes/v1-rebuild/tasks.md`

任务状态更新使用：
```bash
# 查看任务列表
/task list

# 更新任务状态
/task update <id> --status in_progress|completed
```

---

## 规范文档

详细规范请参阅 `.claude/rules/` 目录：

| 规范 | 文件 |
|------|------|
| 代码规范 | `.claude/rules/coding-standards.md` |
| API 设计规范 | `.claude/rules/api-design-standards.md` |
| 测试规范 | `.claude/rules/testing-standards.md` |
| 安全规范 | `.claude/rules/security-standards.md` |
| 开发工作流 | `.claude/rules/development-workflow.md` |
| 前端设计规范 | `.claude/rules/frontend-design-standards.md` |

---

*最后更新：2026-04-01*
